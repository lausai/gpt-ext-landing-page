# AGENTS.md

## 專案概述
這是一個用於 ChatGPT 網站的擴充功能的落地頁

## 技術堆疊
- 使用 next.js 框架

## 編碼規範
- 盡量不要建立只有一個呼叫端而且在5行以內的函式，過多的小函式會增加維護困難度，若是為了可讀性，可用註解替代函式名稱

## 特殊規則
- 程式改完不要使用 npm run build 或是 next build 驗證，因為這些指令非常耗資源
- 使用 npm run lint 檢查程式碼時 timeout 時間設定為 35 秒，因為執行環境可能會是 WSL 系統，lint 時間會很長
